<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="/css/global.css" />
    <link
      rel="icon"
      href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>👟</text></svg>"
    />
  </head>
  <body>
    <header class="flex flex-col">
      <nav class="flex justify-between lg:py-6 py-2 border-b-2">
        <h1 class="text-slate-800 text-4xl font-bold"><a href="/">Shopping DEMO</a></h1>
        <a class="text-4xl px-2" href="/cart">🛒</a>
      </nav>
    </header>
    <div class="flex flex-col gap-6">
      <h1 class="text-2xl font-bold text-center text-slate-700 py-6">Thank you for your purchase !!</h1>
      <div class="flex flex-col gap-6">
        <ul class="flex flex-col gap-6">
          <% cart.forEach(({item, size, quantity}) => { %>
            <%- include("cart-item", { item, size, quantity, checkout: true }) %>
          <% }) %>
        </ul>
        <dl class="flex flex-col">
          <div class="flex justify-end gap-2">
            <dt class="font-bold">Subtotal:</dt>
            <dd>$<%= subtotal %>.00</dd>
          </div>
          <div class="flex justify-end gap-2">
            <dt class="font-bold">Shipping:</dt>
            <dd>$<%= shipping %>.00</dd>
          </div>
          <div class="flex justify-end gap-2">
            <dt class="font-bold">Total:</dt>
            <dd>$<%= subtotal + shipping %>.00</dd>
          </div>
        </dl>
      </div>
      <footer class="border-t-2 py-4"><a class="underline before:content-['<<']" href="/">continue shopping</a></footer>
      <ins class="mta_conversion_tag"></ins>
      <script src="<%= MTA_CONVERSION_TAG_URL %>" budget="<%= subtotal + shipping %>"></script>
    </div>
  </body>
</html>
